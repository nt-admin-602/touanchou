# 灯案帳 spec.md

## 1. アプリ概要

### アプリ名

**灯案帳**  
読みは仮に「とうあんちょう」。

### 目的

トルコランプ制作ワークショップで、実際にガラスを貼る前に、スマホ上で図案を試行錯誤するためのWeb/PWAアプリ。

ワークショップでは、まず紙の上にデザインし、接着剤をつけてから、デザインしたガラスを置いていく。  
このアプリは、その「紙の上で図案を考える工程」をスマホ上で補助する。

### 主用途

- 平面の長方形キャンバス上に、ガラス片を配置して図案を作る
- 四角、三角、ひし形のガラスを使う
- 円形配置、鏡像配置、パターン連続配置などを補助する
- 作成した図案をスマホで見返す
- 図案をPNG画像として共有する
- 図案データをJSONでエクスポート/インポートしてバックアップ・移行する

### 非目的

- 実際のランプ形状を3D再現すること
- 印刷用の実寸高精度出力
- サーバー保存
- ログイン/アカウント管理
- アプリストア配布

---

## 2. 実装方針

### 基本方針

- スマホ向けWeb/PWAとして作る
- アプリストア登録はしない
- Vercelにデプロイする
- URLを知っていれば誰でも使える
- データは端末ローカルに保存する
- サーバー保存はしない
- ログインしない
- UI表示言語は日本語
- 内部コード、変数名、型名、ファイル名は英語

### 技術選定

以下で実装済み。

- **フレームワーク**: React + TypeScript
- **ビルド**: Vite
- **描画**: SVG（キャンバスを SVG で描画、座標は mm 単位）
- **状態管理**: Zustand
- **DB**: IndexedDB（idb ライブラリ）
- **PWA**: vite-plugin-pwa
- **CSS**: Tailwind CSS v4

### 最優先事項

**操作感最優先。**

多少実装が複雑になっても、スマホで触ったときに以下が快適であることを優先する。

- タップ配置
- ドラッグ移動
- 回転ハンドルによる自由回転
- ピンチズーム
- 2本指パン
- 小さいガラスの選択
- フローティングツール操作

---

## 3. Phase構成

実装は段階分けする。  
この仕様書は1ファイルにまとめるが、Claude CodeにはPhaseごとに順番に実装させる。

### Phase 1: 操作感プロトタイプ + PWA土台 ✅ 完了

目的:  
保存や高度な補助機能より先に、スマホで触ったときの操作感を確認する。

含める機能:

- PWA基本設定
- ホーム画面追加対応
- スマホ縦画面UI
- 新規作成画面
- S/M/自由入力キャンバス選択
- 編集画面
- 白背景 + 1mmグリッド
- 24色パレット
- 3形状パレット
  - ひし形
  - 三角形
  - 正方形
- 色・形を選んでタップ配置
- 配置済みガラスの選択
- ドラッグ移動
- 回転ハンドルによる自由回転
- ピンチズーム
- 2本指パン
- 選択中ガラスの最前面表示
- 選択枠
- フローティングツールの仮表示

完了条件:

- スマホブラウザで開ける
- ホーム画面に追加できる
- スマホ縦画面でキャンバスが操作できる
- 3形状・24色を選んで配置できる
- 配置したガラスを選択、移動、回転できる
- ピンチズームと2本指パンが自然に動く

### Phase 2: 編集の土台 ✅ 完了

含める機能:

- 色変更
- 形変更
- 削除
- 複数選択
- 複数選択時のまとめ移動
- 複数選択時の各ガラス中心回転
- Undo / Redo
- キャンバス外配置制限
- 仮配置表示の共通処理

完了条件:

- 選択済みガラスの色・形を下部パレットで変更できる
- ゴミ箱で削除できる
- 複数選択できる
- 複数選択したガラス群をまとめて移動できる
- 複数選択したガラス群をそれぞれの中心で同じ角度だけ回転できる
- Undo / Redo が直近50操作まで機能する

補足: 重なり判定は当初実装したが、星型パターンなど意図的な密着配置を妨げるため後に撤去した（[13. 重なり判定（廃止）](#13-重なり判定廃止)）。

### Phase 3: 保存・管理 ✅ 完了

含める機能:

- 手動保存
- 自動バックアップ
- 未保存バックアップ復元確認
- 図案一覧
- 図案名変更
- 図案削除
- 図案複製
- 最終更新日順の並び替え
- サムネイル表示
- JSONエクスポート
- JSONインポート

完了条件:

- 複数図案を端末ローカルに保存できる
- 保存済み図案を一覧から開ける
- 図案名を変更できる
- 図案を削除・複製できる
- サムネイルで図案を識別できる
- 未保存作業を復元できる
- JSONでバックアップ・復元できる

### Phase 4: 配置補助 ✅ 完了

含める機能:

- 複製
- 鏡像配置
- 放射対称配置
- パターン連続配置
- 各補助機能の仮配置プレビュー
- 確定 / キャンセル
- 展開数・繰り返し数の操作

完了条件:

- 補助配置はすべて仮配置として表示される
- 確定 / キャンセルできる
- 鏡像配置で上下・左右・対角線反転ができる
- 放射対称配置で 2 / 4 / 6 / 8 / 16 展開できる
- 複数選択パターンを方向指定で連続配置できる

### Phase 5: 出力仕上げ ✅ 完了（https://touanchou.vercel.app にデプロイ済み）

含める機能:

- 透過PNGエクスポート
- 図案一覧からのPNGエクスポート
- エクスポート画像の調整
- PWA仕上げ
- Vercelデプロイ確認

完了条件:

- 背景透過PNGを出力できる
- グリッドなしで出力される
- キャンバス全体が出力範囲になる
- スマホで見返し・共有しやすい画像になる
- Vercel上でPWAとして使える

---

## 4. 画面構成

### 4.1 初期画面

アプリ起動時は、新規作成画面を表示する。

表示する選択肢:

- Sサイズ
- Mサイズ
- 自由入力
- 保存済み図案を開く

既存図案を編集したい場合は「保存済み図案を開く」から図案一覧へ進む。

### 4.2 新規作成

新規作成時に入力するのはキャンバスサイズのみ。

- Sサイズ
- Mサイズ
- 自由入力

図案名は新規作成時には入力しない。  
明示的な保存操作時に自動仮名を付ける。

例:

```text
新しい図案 2026-07-07 16:30
```

図案名はあとから変更可能。

### 4.3 編集画面

スマホ縦画面前提。

構成:

- 上部ツールバー
- 中央キャンバス
- 下部パレット
- 選択時フローティングツール

### 4.4 上部ツールバー

常時表示:

- 保存
- Undo
- Redo
- メニュー

メニュー内:

- 図案一覧へ戻る
- PNGエクスポート
- JSONエクスポート
- JSONインポート
- 新規作成
- 図案名変更
- その他設定

上部ツールバーはコンパクトにする。

### 4.5 下部パレット

色パレットと形パレットを常時表示する。

形パレット:

- ひし形
- 三角形
- 正方形

色パレット:

- 24色
- 横スクロール
- 現在選択中の色をハイライト表示

挙動:

- ガラス未選択時:
  - 選んだ色・形で新規配置する
- ガラス選択時:
  - 色をタップすると選択ガラスの色変更
  - 形をタップすると選択ガラスの形変更

### 4.6 フローティングツール

ガラスを選択すると、その近くに表示する。

表示位置:

- 選択ガラス、または選択ガラス群の近く
- ガラスの上方に優先配置、画面からはみ出る場合は下方に自動切替
- 下部パレットとは重ならないようにする

3列グリッドレイアウト（2行）で表示。有効な操作を先頭に配置する。

行1（有効）:

- ⊞ 複数選択トグル
- 🗑 削除
- ⧉ 複製

行2（配置補助）:

- ⇋ 鏡像配置
- ⋯ 連続配置
- ✳ 放射対称配置

ボタン表記：

- アイコンのみ
- MVPではツールチップやチュートリアルは実装しない

---

## 5. キャンバス仕様

### 5.1 キャンバス

- 平面の長方形キャンバス
- 白背景
- 1mm単位グリッド
- 内部座標はmm単位
- 画面表示時にmmをpxへ変換する

### 5.2 グリッド

- 1mm単位のグリッドを表示する
- 配置・移動時は1mmグリッドにスナップする
- 内部座標もmmベースで管理する
- ズーム倍率が低いときは、表示上は5mmや10mmごとに目立つ線を出してよい
- スナップ自体は常に1mm単位

### 5.3 キャンバス操作

**スマホ / タッチ操作:**

- ピンチイン / ピンチアウトで拡大縮小
- 2本指ドラッグでキャンバス全体を移動
- 1本指操作は、配置・選択・移動・回転に使う
- ズーム中でも配置・選択・ドラッグ操作ができる

**PC / マウス操作:**

- マウスホイール: カーソル中心にズームイン/アウト
- 右クリックドラッグ: キャンバスをパン
- 中ボタンドラッグ: キャンバスをパン
- 左クリック: ガラス配置・選択（スマホタッチと同じ）

### 5.4 キャンバス外配置

- ガラスの中心点がキャンバス内にあれば配置可能
- ガラスの一部がキャンバス外にはみ出しても許容する
- 中心点がキャンバス外に出る配置は不可
- 確定時・移動時・仮配置確定時に中心点がキャンバス内か確認する

### 5.5 キャンバス初期表示

- 縦幅フィット：画面の利用可能な縦幅にキャンバス高さを合わせてズーム
- 左端揃え：初期表示でキャンバス左端が画面左端近くに揃えられる
- 横に長いキャンバスは左端から開始しパンで右側を確認する

### 5.6 パン制限

- すべての辺で同一の最低マージン（80px）を強制
- パンが制限値を越えると自動で提源（吸着）する
- キャンバスが宸在することを常に情報として提供する
- 定数 `VIEWPORT_MARGIN_PX = 80` で調整可能

---

## 6. キャンバスサイズ

### 6.1 サイズ選択

新規作成時に以下から選ぶ。

- Sサイズ
- Mサイズ
- 自由入力

### 6.2 自由入力

- 幅mmを入力
- 高さmmを入力
- 入力値をもとに長方形キャンバスを作成する

入力制限:

- 最小値・最大値は実装者判断
- ただしスマホで操作できない極端なサイズは防ぐ
- 不正な値・極端に小さい値・大きすぎる値にはバリデーションを入れる
- 入力エラー時は作成できないようにする

### 6.3 S/Mテンプレート

実寸は未確定。  
以下は会話からの仮仕様。

Mサイズ:

- ひし形を縦置きで、縦約7個・横約30個並ぶくらい
- WSで見たMサイズの目安

Sサイズ:

- Mサイズの約75%
- 感覚値
- 後からコード上の設定値を調整できるようにする

テンプレート算出方針:

- 固定寸法を直接書くのではなく、形状設定と目地幅から仮計算する
- `shapeConfig.diamond` の寸法を使う
- `DEFAULT_GROUT_GAP_MM` は 2mm
- Mサイズ:
  - diamond縦置き 7個分の高さ
  - diamond横方向 30個分の幅
- Sサイズ:
  - Mサイズの 0.75 倍

---

## 7. ガラス形状仕様

### 7.1 MVPで扱う形状

- ひし形
- 三角形
- 正方形

将来的に追加できるようにする:

- 丸ビーズ
- 長方形
- 雫型
- その他のWS素材

形状定義はハードコードしすぎず、後からShape定義を追加しやすい構造にする。

### 7.2 形状サイズ方針

ユーザー記憶に基づく仮仕様。  
実寸はうろ覚えのため、コード上の設定値として後から調整できるようにする。

- ひし形と三角形は、一辺の長さが同じになるように扱う
- 三角形は正三角形
- ひし形は6枚で星型に配置できる60°/120°のロゼンジ型
- 正方形は7mm角

### 7.3 ひし形

- 6枚で星型・花型にぴったり配置できる形
- 60°/120°のロゼンジ型
- 長い対角線が約10mmという記憶がある
- ただし、三角形との辺長一致を優先するため、実装では `sideLengthMm` を共通値として扱う
- 実寸は後で調整可能にする

### 7.4 三角形

- 正三角形
- 高さは約10mmという記憶がある
- ただし、ひし形との辺長一致を優先するため、実装では `sideLengthMm` を共通値として扱う
- 実寸は後で調整可能にする

### 7.5 正方形

- 7mm × 7mm
- サイズ固定
- 配置時の自由拡大縮小はしない

### 7.6 Shape設定例

```ts
const SHAPE_CONFIG = {
  diamond: {
    type: "rhombus",
    sideLengthMm: 10, // 仮値。後で調整可能
    acuteAngleDeg: 60,
  },
  triangle: {
    type: "equilateralTriangle",
    sideLengthMm: 10, // diamond と共通想定。仮値
  },
  square: {
    type: "square",
    sideLengthMm: 7,
  },
}
```

注意:

- `sideLengthMm: 10` は仮値
- 実寸根拠は弱い
- UIではサイズ変更しない
- 後からコード上の設定値を修正する

---

## 8. 目地幅

### 8.1 方針

- ガラス片同士は密着させない
- 隙間に石膏が入ることで美しく見えるため、目地込みの配置を前提にする
- 目地幅の初期値は2mm
- UIでは変更しない
- コード上の設定値として扱う

```ts
const DEFAULT_GROUT_GAP_MM = 2
```

### 8.2 目地幅の使用箇所

- 連続配置の間隔計算
- 放射対称配置の補助計算
- S/Mテンプレート寸法計算
- 自動配置補助

### 8.3 重なりとの関係

- 重なり判定自体を行わないため（[13. 重なり判定（廃止）](#13-重なり判定廃止)）、目地幅の有無が配置の可否に影響することはない
- 最終的な目地感は、実制作時の手張りで調整できる前提

---

## 9. 色パレット

### 9.1 方針

- 固定24色パレット
- 初期色は透明
- 明示的に色を選ばない場合も透明
- 鏡は24色のうち1色として扱う
- 鏡専用の材質切り替えUIは作らない

### 9.2 24色パレット

```text
1. 透明
2. 鏡
3. 赤
4. ワインレッド
5. 黄色
6. 半透明黄色
7. オレンジ
8. 黄緑
9. 緑
10. 半透明エメラルドグリーン
11. 茶色
12. ベージュ
13. 青
14. 水色
15. 半透明水色
16. 半透明スカイブルー
17. 紺
18. ターコイズ
19. 紫
20. 半透明ラベンダー
21. 半透明ピンク
22. グレー
23. クリアブラック
24. ブラック
```

### 9.3 表示

通常ガラス:

- 半透明表示
- 背景グリッドが少し透ける

鏡ガラス:

- 反射っぽいグラデーション表示
- 単なる灰色ベタ塗りではなく、ミラー感を出す
- 完全なリアル反射までは不要
- 通常ガラスと見分けがつくことを優先する

クリア/半透明系:

- 通常色より透明度を高めに表示する

### 9.4 Color設定例

```ts
type GlassColor = {
  id: string
  label: string
  fill: string
  opacity: number
  isMirror?: boolean
}
```

---

## 10. データ構造

### 10.1 座標単位

- 図案データ上の位置・サイズはmm単位で保持する
- 画面表示時に mm → px へ変換する
- スマホ画面サイズやズーム倍率に依存しないデータにする

### 10.2 ガラス座標

- `xMm`, `yMm` はガラスの中心点
- 回転は中心点を基準に行う
- 移動時も中心点座標を更新する
- 鏡像配置・放射対称配置・連続配置も中心点ベースで計算する

### 10.3 型例

```ts
type ShapeType = "diamond" | "triangle" | "square"

type GlassItem = {
  id: string
  shape: ShapeType
  colorId: string
  xMm: number
  yMm: number
  rotationDeg: number
}

type DesignDocument = {
  id: string
  name: string
  createdAt: string
  updatedAt: string
  canvasWidthMm: number
  canvasHeightMm: number
  groutGapMm: number
  items: GlassItem[]
  dataVersion: number
}

type DraftBackup = {
  document: DesignDocument
  undoStack: HistoryEntry[]
  redoStack: HistoryEntry[]
  updatedAt: string
}
```

### 10.4 JSONエクスポート

JSONには以下を含める。

- 図案名
- 作成日
- 更新日
- キャンバスサイズ
- ガラス配置データ
  - id
  - shape
  - colorId
  - xMm
  - yMm
  - rotationDeg
- データ形式バージョン
- 必要ならサムネイル情報

JSONインポート時:

- インポートした図案は、常に新しい図案として追加する
- 既存図案とは別IDを発行する
- 同じ図案名が存在していても上書きしない
- 上書き確認ダイアログは出さない
- 必要ならユーザーが後から名前変更・削除する

---

## 11. 基本操作

### 11.1 新規配置

- 色を選ぶ
- 形を選ぶ
- キャンバスをタップして配置する
- 1mmグリッドにスナップする
- 新規配置時の初期向きは形ごとに固定

初期向き:

- ひし形: 縦長
- 三角形: 頂点が上
- 正方形: 傾きなし

新規配置時は毎回この向きで置く。  
必要なら配置後に回転ハンドルで調整する。

### 11.2 選択

- 配置済みガラスをタップすると選択状態になる
- 選択中ガラスは最前面に表示する
- 選択枠を表示する
- 回転ハンドルを表示する
- フローティングツールを表示する

選択解除:

- キャンバスの空白部分をタップすると選択解除
- 選択中のガラスをもう一度タップすると選択解除
- 複数選択中の場合も、空白タップで全選択解除

### 11.3 当たり判定

- ガラスの選択判定は、見た目の図形そのものを対象にする
- 図形の外側をタップしても選択されない
- 当たり判定を見た目より大きくしない
- 小さいガラスを選びにくい場合は、ピンチズームして操作する前提
- 見た目と操作対象が一致することを優先する

### 11.4 色変更

選択状態で下部カラーパレットをタップすると、選択中ガラスの色を変更する。

複数選択時は、選択中のガラスすべての色を変更する。

### 11.5 形変更

選択状態で下部形状パレットをタップすると、選択中ガラスの形を変更する。

複数選択時は、選択中のガラスすべての形を変更する。

### 11.6 移動

単体選択時:

- 選択ガラスをドラッグで移動する
- 1mmグリッドにスナップする

複数選択時:

- 選択中のいずれかのガラスをドラッグすると、選択中ガラス全体がまとめて移動する
- ガラス同士の相対位置は維持する
  - 移動量（ドラッグの差分）を1mm単位にスナップしてから全アイテムへ同じ量を加える
  - 各アイテムの座標を個別にスナップすると、放射対称・鏡像配置由来の非整数mm位置のアイテム同士で相対位置がずれ、マージン幅が不揃いになるため
- 連続配置用のパターン調整にも使える

### 11.7 回転

- 選択中ガラスに回転ハンドルを表示する
- 回転ハンドルをドラッグすると **5度単位** でスナップしながら回転する
- スマホで指操作しやすいサイズ・位置にする
- ガラス本体を隠しすぎない
- 誤って移動操作になりにくいようにする

定数 `ROTATION_SNAP_DEG = 5` で調整可能。

単体選択時:

- 選択ガラスの中心点を基準に回転する

複数選択時:

- 選択中の各ガラスが、それぞれ自分自身の中心点を基準に回転する
- 位置関係は変えない
- 全ガラスに同じ回転量を適用する
  - 回転量（ドラッグ差分の角度）を5度単位にスナップしてから全アイテムへ同じ量を加える
  - 各アイテムの回転角を個別にスナップすると、非整数角度のアイテム同士で相対角度がずれるため
- パターン全体を外接範囲中心で回す動きではない

### 11.8 削除

- 選択中のガラスをゴミ箱ボタンで削除する
- 削除確認ダイアログは出さない
- 複数選択中の場合は、選択中のガラス群をまとめて削除する
- 削除操作は Undo 対象にする
- 誤って削除した場合は Undo で戻せる

---

## 12. 複数選択

### 12.1 開始

- 通常時は単体選択
- フローティングツールの ⊞ ボタンを押すと複数選択モードに入る
- 複数選択モード中は、ガラスをタップするたびに選択/解除を切り替える
- 選択モード（☁）でのタップも同様にトグル挙動

### 12.2 終了

- 空白部分をタップすると、全選択解除する
- 同時に複数選択モードも終了する
- もう一度ボタンを押して終了する操作は必須ではない

### 12.3 用途

複数選択したガラス群は、以下の対象になる。

- まとめ移動
- 各ガラス中心回転
- 複製
- 連続配置
- 鏡像配置
- 放射対称配置
- 削除

---

## 13. 重なり判定（廃止）

- ガラス本体同士が重なっているかどうかのチェックは行わない
- 通常の移動・回転、複製・鏡像配置・連続配置・放射対称配置の確定、いずれも重なりを理由に妨げられない
- 星型（6枚のひし形など）のように意図的に辺を密着・重畳させる配置を自由に作れることを優先する
- 過去のMVPでは重なり時に赤枠表示・確定不可・元の位置へ差し戻す挙動があったが、すべて撤去した

---

## 14. 仮配置共通仕様

### 14.1 対象ツール

- 複製
- 鏡像配置
- 連続配置
- 放射対称配置

### 14.2 共通フロー

1. ツール実行
2. 仮配置を表示
3. 必要に応じて位置・中心点・基準線などを調整
4. 確定 / キャンセル

### 14.3 表示

仮配置中のガラスは、通常配置済みガラスと区別して表示する。

- 半透明
- 点線枠

確定すると通常表示になる。  
キャンセルすると仮配置を破棄する。

---

## 15. 複製

### 15.1 基本

- 選択中のガラス、または複数選択中のガラス群を複製できる
- 元ガラスは残す
- コピー候補のみ追加する

### 15.2 操作

- 複製ボタンを押すとコピー候補を作成する
- コピー候補は即確定しない
- コピー候補をドラッグして置き場所を決める
- 指を離したあと、確定 / キャンセルを表示する
- キャンセルするとコピー候補を破棄する

---

## 16. 鏡像配置

### 16.1 基本

- 選択中のガラス、または複数選択中のガラス群を鏡像コピーできる
- 元ガラスは残す
- コピー候補だけを追加する
- いきなり確定せず、仮配置プレビューを表示する
- 確定 / キャンセルできる

### 16.2 反転軸

反転軸を選べる。

- 上下
- 左右
- 対角線 `/`
- 対角線 `\`

### 16.3 基準

- 初期の反転基準はキャンバス中央
- 反転基準線を画面上に表示する
- 基準線はドラッグで移動できる
- 基準線を動かすと仮配置プレビューもリアルタイム更新する

### 16.4 対角配置との関係

「対角配置」という独立ツールは作らない。  
鏡像配置の反転軸として扱う。

つまり、対角配置は以下で実現する。

- 対角線 `/` 反転
- 対角線 `\` 反転

---

## 17. 放射対称配置

### 17.1 基本

円形配置というより、放射対称・鏡像配置に近い。

- 選択中のガラスを基準に、キャンバス中央を中心として対称位置にコピー候補を出す
- コピーは単純な回転コピーではなく、鏡像コピーを基本にする
- 上下左右・斜め方向に展開したとき、元ガラスと対称になる向きで配置する
- トルコランプらしい左右対称・上下対称の模様を作りやすくする
- 元ガラスは残す
- コピー候補のみ追加する
- 仮配置プレビュー → 確定 / キャンセル

### 17.2 展開数

選択できる展開数:

- 2
- 4
- 6
- 8
- 16

例:

- 展開数2:
  - 上側に置いたガラスに対して、下側に上下反転したコピー候補を出す
- 展開数4:
  - 下側、左、右にコピー候補を出す
- 展開数6 / 8 / 16:
  - 円周上に等間隔でコピー候補を出す

### 17.3 中心点

- 初期中心点は、選択ガラス（群）の頂点のうち、今画面に表示されている範囲内で最も空いている方向（上下左右のうち表示範囲の端までの距離が最大の方向）にある頂点にする
  - ひし形6枚を星型に並べるような、頂点同士をきっちり合わせたい配置を素直に作れることを優先する
  - キャンバスが表示範囲より大きく、パン・ズームで一部しか見えていない場合は、実際に見えている範囲を基準に判定する
  - キャンバス外にはみ出す場合は上下左右それぞれキャンバス境界内にクランプする
- 放射対称配置ツール起動時に中心点を表示する
- 中心点はドラッグで移動可能
- 中心点を動かすと、仮配置プレビューもリアルタイム更新する
- フローティングツールは、選択ガラスだけでなく中心点・展開プレビュー全体にも被らない位置へ自動調整する

### 17.4 向き

- コピーされたガラスは、初期配置ガラスの向きを基準にする
- コピー先の位置に応じて鏡像反転した向きにする
- 単純回転よりも、左右対称・上下対称に見えることを優先する

---

## 18. パターン連続配置

### 18.1 基本

- 複数選択したガラス群を1パターンとして扱う
- そのパターンを一定方向に繰り返し配置できる
- 元のガラス群は残す
- コピーだけを追加する
- 実行前に仮配置プレビューを表示し、確定 / キャンセルできる

### 18.2 方向指定

方向ボタンで繰り返し方向を選ぶ。

- 上
- 下
- 左
- 右
- 左上
- 右上
- 左下
- 右下

### 18.3 繰り返し数

- デフォルトは4
- 「−」「＋」ボタンで増減できる
- 数値入力よりもタップ操作を優先する
- スマホ操作前提のため、大きめの増減ボタンを使う

### 18.4 間隔

- 複数選択したガラス群の外接範囲を1パターンとして扱う
- 繰り返し時は、そのパターンの一番端を基準に次のコピー位置を決める
- パターン同士の間には目地幅を空ける
- 目地幅の初期値は2mm
- つまり「選択パターンの端 + 目地幅」の位置に次のパターンを置く

例:

```text
[選択パターン] 2mm [コピー1] 2mm [コピー2] 2mm [コピー3]
```

### 18.5 向き

- コピーは同じ向きのまま繰り返す
- 交互反転はMVPでは不要

---

## 19. Undo / Redo

### 19.1 基本

- Undo: 直前の操作を戻す
- Redo: Undoした操作をやり直す
- 図案の試行錯誤が目的なので必須機能として扱う
- 履歴は直近50操作まで保持する
- 50件を超えた古い履歴は破棄する
- スマホで重くならない範囲を優先する

### 19.2 対象操作

- 配置
- 移動
- 回転
- 色変更
- 形変更
- 削除
- 複製確定
- 連続配置確定
- 鏡像配置確定
- 放射対称配置確定

### 19.3 保存との関係

- 通常の保存操作をしたら、Undo / Redo 履歴はクリアする
- 保存済み図案を開いた直後は、Undo / Redo 履歴なしの状態から開始する
- 未保存の自動バックアップでは、図案データと同じように Undo / Redo 履歴も保持する
- 誤ってブラウザを閉じて復元した場合、作業途中の状態と履歴も復元できる

手動保存:

- 図案データを保存
- Undo / Redo 履歴はクリア

自動バックアップ:

- 図案データを一時保存
- Undo / Redo 履歴も一時保存

バックアップ復元:

- 図案データを復元
- Undo / Redo 履歴も復元

---

## 20. 保存・図案管理

### 20.1 保存方針

- 複数の図案をスマホ内ローカルに保存できる
- 図案ごとに名前を付けられる
- 図案一覧から再編集できる
- 各図案には作成日・更新日を持たせる
- サーバー保存・ログインは不要

### 20.2 手動保存

- 基本はユーザーが保存ボタンを押して保存する
- 保存済み図案として残すには、明示的な保存操作が必要
- 初回保存時、図案名は自動で仮名を付ける
- 図案名はあとから変更できる
- 保存操作後、Undo / Redo 履歴はクリアする

### 20.3 自動バックアップ

- 編集中の内容は自動バックアップする
- 誤ってブラウザを閉じた場合でも、未保存の作業内容を復元できる
- 自動バックアップは保存済み図案とは別扱いにする
- 自動バックアップには図案データと Undo / Redo 履歴を含める

保存タイミング:

- 編集内容に変更があった場合、自動バックアップ対象にする
- 操作直後すぐには保存しない
- 最後の操作から1〜2秒程度経過したらバックアップ保存する
- 連続操作中は保存を待つ
- スマホでの操作感を優先し、バックアップ処理でUIを重くしない

### 20.4 復元

- アプリ起動時に未保存バックアップがある場合、復元するか確認する
- 復元する場合は、前回の編集途中状態を開く
- 復元しない場合は、新規作成画面へ進む

### 20.5 ローカル保存方式

保存方式は実装者判断とする。

- LocalStorage / IndexedDB などは固定しない
- ただし以下に耐える方式を選ぶ
  - 複数図案の保存
  - 図案名・作成日・更新日・キャンバスサイズの保存
  - ガラス配置データの保存
  - サムネイル保存
  - 自動バックアップ
  - Undo / Redo 履歴の一時保存
  - スマホブラウザで安定して動作すること

必要なら IndexedDB を使ってよい。

### 20.6 図案一覧

保存済み図案を一覧表示する。

表示項目:

- サムネイル
- 図案名
- キャンバスサイズ
- 作成日
- 更新日

並び順:

- 最終更新日順

各図案に対してできる操作:

- 開く
- 名前変更
- 削除
- 複製
- PNGエクスポート
- JSONエクスポート

---

## 21. PNGエクスポート

### 21.1 用途

- スマホで見返す
- LINEなどで共有する
- 制作時に図案として参照する

### 21.2 出力仕様

- PNG形式
- 背景透過
- グリッドなし
- キャンバス全体を出力
- 余白も含めて、図案の位置関係を保持する
- 配置済みガラスだけを描画する
- 出力サイズは画面表示と同程度でよい
- 高解像度出力や倍率指定はMVPでは不要

---

## 22. PWA・公開

### 22.1 PWA

Phase 1からPWAとして実装する。

- スマホブラウザで開ける
- ホーム画面に追加してアプリっぽく起動できる
- manifest / service worker を用意する
- 基本画面・アイコン・起動表示をPWA前提で整える
- ローカル保存、自動バックアップと相性のよい構成にする

### 22.2 アイコン

- 正式アイコンは後で別途用意する
- MVPではPWAとして動かすための仮アイコンを用意する
- 仮アイコンの見た目は作り込まない
- アイコン制作を主タスクに含めない

### 22.3 デプロイ

- Vercelにデプロイする
- URLを知っていれば誰でも使える
- ログインなし
- アカウント登録なし
- サーバー保存なし
- 図案データは各ユーザーの端末ローカルに保存する
- 他人の図案データは共有されない

---

## 23. UIデザイン方針

### 23.1 全体

- シンプル実用重視
- 白背景
- 薄い線
- 余計な装飾は入れない
- トルコランプ風の華やかな装飾より、図案作成のしやすさを優先する
- ガラス色そのものが主役になるようにする

### 23.2 選択中表示

- 選択中ガラスには選択枠を表示する
- 選択枠の具体的な色・線種は実装者判断
- ただしスマホ画面で見やすいことを優先する
- 背景・ガラス色・半透明ガラス・鏡ガラスの上でも判別しやすい表示にする
- 回転ハンドルとフローティングツールも同時に表示する
- 複数選択時は、選択中ガラス群それぞれに選択枠を表示する
- フローティングツールは選択ガラス群の近くに1つだけ表示する

### 23.3 描画順

- 通常時のガラスは標準の描画順で表示する
- 選択中のガラスは最前面に表示する
- 複数選択中の場合、選択中のガラス群を最前面に表示する
- 仮配置中のガラスも、確認しやすいように前面寄りに表示する
- 重なりを妨げる仕組みはないため、ガラス同士が重なった場合の見やすさ・操作対象の分かりやすさは描画順（最前面表示）で担保する

---

## 24. 実装上の注意

### 24.1 操作感

このアプリで最も重要なのは操作感。  
保存や高度な補助機能より先に、スマホでのタッチ操作を検証すること。

特に注意する操作:

- 1本指タップ配置
- 1本指ドラッグ移動
- 回転ハンドルドラッグ
- ピンチズーム
- 2本指パン
- 小さいガラスの正確な選択
- フローティングツールの押しやすさ

### 24.2 mm座標とpx変換

内部データはmm基準。  
表示時だけpxへ変換する。

ズーム・パンは表示座標の変換として扱い、保存データはmm座標のまま保つ。

### 24.3 形状サイズ

形状サイズはユーザー記憶に基づく仮値。  
実物確認後にコード上で調整できるようにする。

アプリ内にサイズ調整UIは不要。

### 24.4 未確定事項

以下は確定実寸ではない。

- Mサイズ実寸
- Sサイズ実寸
- ひし形・三角形の正確な辺長
- ひし形の正確な横幅
- 実際のWS素材の全色

ただし、現時点の仕様では以下を採用する。

- Mサイズは「ひし形縦置きで縦7個・横30個程度」
- SサイズはMサイズの75%
- ひし形と三角形は一辺の長さを共通にする
- 正方形は7mm角
- 目地幅は2mm
- 色パレットは24色固定

---

## 25. 最初にClaude Codeへ依頼する内容

まずはPhase 1だけを実装する。

依頼文例:

```text
spec.md を読んで、まず Phase 1: 操作感プロトタイプ + PWA土台 だけを実装してください。

優先事項:
- スマホでの操作感を最優先
- PWAとしてホーム画面追加できる
- 3形状・24色を選んでキャンバスに配置できる
- 配置済みガラスを選択、移動、回転できる
- ピンチズームと2本指パンが自然に動く
- 内部座標はmm、表示時にpx変換
- 保存・Undo/Redo・高度な配置補助はPhase 1では未実装でよい

Phase 1が終わったら、実装内容と未実装項目を報告してください。
```

---

## 26. ファイル名

この仕様書ファイル名:

```text
spec.md
```
